[[bin]]
name = "mscikdf_cli"
path = "src/cli.rs"

[package]
name = "mscikdf-playground"
version = "1.0.0"
edition = "2021"

[dependencies]
libloading = "0.9.0"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"          # 减小二进制体积
strip = true
